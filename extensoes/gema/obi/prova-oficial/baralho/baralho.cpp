#include <bits/stdc++.h>
using namespace std;

int main()
{
    vector<int> v;
    string s;

    cin >> s;

    for (int i = 0; i < 50; i++)
    {
        v.push_back() = s[i];
    }

    for (int i : v)
    {
        cout << v[i] << ' ';
    }

    return 0;
}